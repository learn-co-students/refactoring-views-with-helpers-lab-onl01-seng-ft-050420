<ul>
<% @songs.each do |song| %>
  <li>Song: <%= link_to song.title, song_path(song)%> </li>
  <ul>
    <li>Artist: <%= display_artist(song) %></li>
  </ul>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
